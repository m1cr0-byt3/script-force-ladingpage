{
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && npm run css && npm run js && npm run html && npm run copy",
    "css": "sass --style=compressed assets/scss:dist/assets/css",
    "js": "mkdir -p dist/assets/js && terser assets/js/main.js -c -m -o dist/assets/js/main.js",
    "html": "html-minifier-terser --collapse-whitespace --remove-comments --minify-css true --minify-js true -o dist/index.html index.html",
    "copy": "cp -R assets/images dist/assets/ && cp -R assets/vendor dist/assets/ 2>/dev/null || true"
  },
  "devDependencies": {
    "sass": "^1.70.0",
    "terser": "^5.24.0",
    "html-minifier-terser": "^7.2.0"
  }
}
